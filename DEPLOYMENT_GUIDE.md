# 🚀 คู่มือ Deploy 3 Web Apps (3-in-1 Pattern)

## 📋 ภาพรวม

ระบบนี้ใช้ **Router Pattern** ทำให้ Deploy 1 ครั้ง แต่ได้ 3 หน้าต่างๆ

---

## 🌐 3 หน้าที่มี:

| # | หน้า | URL Parameter | ไฟล์ |
|---|------|---------------|------|
| 1 | **บันทึกข้อมูล** | (default) | `Index.html` |
| 2 | **AI Report** | `?page=ai` | `AI_View.html` |
| 3 | **แบบประเมิน** | `?page=feedback` | `Feedback_Form.html` |

---

## 🔧 ขั้นตอนการ Deploy

### 1. Push โค้ดขึ้น Google Apps Script

```bash
cd "t:\web\DGA APPSCRIPTS"
clasp push
```

### 2. Deploy เป็น Web App

**วิธีที่ 1: ใช้ clasp (Command Line)**
```bash
clasp deploy
```

**วิธีที่ 2: Deploy ผ่าน Apps Script Editor**

1. เปิด Apps Script Editor:
   ```bash
   clasp open
   ```

2. ใน Editor:
   - คลิก **Deploy** (มุมบนขวา)
   - เลือก **New deployment**
   - Type: **Web app**
   - ตั้งค่า:
     - Execute as: **Me**
     - Who has access: **Anyone** (สำหรับ public) หรือ **Anyone with Google account**
   - คลิก **Deploy**
   - **คัดลอก URL** ที่ได้

---

## 🎯 การใช้งาน

สมมติว่า URL ที่ deploy ได้คือ:
```
https://script.google.com/macros/s/ABCD1234.../exec
```

### เข้าหน้าต่างๆ:

#### 1️⃣ **หน้าบันทึกข้อมูล (Main)**
```
https://script.google.com/macros/s/ABCD1234.../exec
```
หรือ
```
https://script.google.com/macros/s/ABCD1234.../exec?page=main
```

#### 2️⃣ **หน้า AI Report**
```
https://script.google.com/macros/s/ABCD1234.../exec?page=ai
```

#### 3️⃣ **หน้าแบบประเมิน**
```
https://script.google.com/macros/s/ABCD1234.../exec?page=feedback
```

---

## 📝 สร้าง Short URLs (ทำให้สั้นกว่าเดิม)

ใช้ **Google URL Shortener** หรือ **Bitly**:

```
https://bit.ly/rice-data       → หน้าบันทึกข้อมูล
https://bit.ly/rice-ai         → หน้า AI Report  
https://bit.ly/rice-feedback   → หน้าแบบประเมิน
```

---

## 🔄 การ Update โค้ด

เมื่อแก้ไขโค้ด:

```bash
# 1. Push ใหม่
clasp push

# 2. Deploy version ใหม่
clasp deploy

# หรือใช้ URL เดิม (auto-update)
# ไม่ต้องทำอะไร ระบบจะใช้โค้ดล่าสุดอัตโนมัติ
```

---

## ⚠️ หมายเหตุสำคัญ

### การ Deploy แบบ Test vs Production

**Test Deployment (HEAD):**
- ใช้โค้ดล่าสุดอัตโนมัติ
- ไม่ต้อง deploy ใหม่
- URL: `...dev` (มี dev ท้าย URL)

**Production Deployment (Version):**
- ต้อง deploy ทุกครั้งที่แก้ไข
- มี version number
- URL: `...exec` (ไม่มี dev)

**แนะนำ:** ใช้ **HEAD deployment** สำหรับ development ใช้โค้ดล่าสุดตลอด

---

## 🛠️ Troubleshooting

### ปัญหา: URL ไม่อัพเดต

**แก้ไข:**
1. Clear browser cache
2. เปิด Incognito mode
3. Deploy version ใหม่

### ปัญหา: Permission denied

**แก้ไข:**
1. ใน Deploy settings → Execute as: **Me**
2. Grant permissions ใหม่

---

## 📚 สรุป

✅ **Deploy 1 ครั้ง** = ได้ 3 หน้า  
✅ **URL เดียว** แต่เพิ่ม `?page=ai` หรือ `?page=feedback`  
✅ **แก้ไขง่าย** อยู่ในโปรเจคเดียวกัน  
✅ **Maintain ง่าย** ไม่ต้องดูแลหลายโปรเจค
